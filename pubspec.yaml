name: adaptive_network_image
description: A Flutter widget that displays images from external URLs on web, handling CORS restrictions with a multi-strategy fallback approach.
version: 0.1.3
homepage: https://primecodesolution.github.io/adaptive_network_image/
repository: https://github.com/PrimeCodeSolution/adaptive_network_image
topics:
  - cors
  - image
  - web
  - flutter-web

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  web: ^1.1.1
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

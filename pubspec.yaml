name: adaptive_network_image
description: A Flutter widget that displays images from external URLs on web, handling CORS restrictions with a multi-strategy fallback approach.
version: 0.1.0
homepage: https://github.com/PrimeCodeSolution/adaptive_network_image
repository: https://github.com/PrimeCodeSolution/adaptive_network_image
topics:
  - cors
  - image
  - web
  - flutter-web

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  web: ^1.1.0
  http: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
